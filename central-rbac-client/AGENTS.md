# AI Agent Instructions — Central RBAC Integration

> **Status**: TODO Phase 2 (plan 260915-1317-central-rbac-client-suite).
> Prescriptive instructions for AI agents integrating Central RBAC SDK
> into app codebases. Full content populated in Phase 2.

## Placeholder outline

1. **Detection**: identify stack from package.json / pyproject.toml → route to correct SDK
2. **MUST rules**: link to SECURITY_INVARIANTS.md
3. **Integration steps**: install → init → wire adapter → protect routes
4. **Verification**: run CONFORMANCE_TESTS runner
5. **Escalation**: ambiguous → ask human, do NOT guess

See `nodejs/AGENTS.md` and `python/AGENTS.md` for stack-specific hints.
