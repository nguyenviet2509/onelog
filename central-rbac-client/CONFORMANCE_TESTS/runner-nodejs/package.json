{
  "name": "@onelog/central-rbac-conformance-nodejs",
  "version": "0.2.0",
  "private": true,
  "description": "Conformance test runner for @onelog/central-rbac-client (Node). Vitest suite against Fastify mock Central.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@onelog/central-rbac-client": "file:../../nodejs",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
