"""adapters — Framework-specific integration helpers.

Each adapter is optional; import errors surface at adapter import time,
not at package import time. Install extras to enable:
    pip install "onelog-central-rbac-client[fastapi]"
    pip install "onelog-central-rbac-client[django]"
    pip install "onelog-central-rbac-client[flask]"
"""
