# Alertmanager — Telegram receivers for OneLog (MCP-only rollout).
#
# Pipeline: vmalert (LogsQL stats) → alertmanager → Telegram Bot API.
# Triage flow: ops nhận alert → mở VMUI / dùng Claude+MCP.
#
# Two receivers differ ONLY by send_resolved (anti RESOLVED noise for
# one-shot events). Route matches `notify_style=event` label (set in rules.yml
# for instant/event-style rules).
#
# Token + chat_id render từ infra/.env (TELEGRAM_BOT_TOKEN, TELEGRAM_ALERT_CHAT_ID)
# qua sed entrypoint trong docker-compose. KHÔNG run alertmanager trực tiếp trên
# file này — placeholder chưa thay.

global:
  resolve_timeout: 5m

route:
  receiver: telegram-trend          # default: burst/trend rules → send RESOLVED
  group_by: ["alertname", "host"]
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 15m
  routes:
    # Event-style rules (OomKillEvent, DiskFullErrors, NewUserOrSudoer):
    # event đã xảy ra rồi, RESOLVED chỉ nói "không có thêm trong 5m" → noise.
    - matchers:
        - notify_style="event"
      receiver: telegram-event

receivers:
  - name: telegram-trend
    telegram_configs:
      - bot_token: "__TELEGRAM_BOT_TOKEN__"
        chat_id: __TELEGRAM_CHAT_ID__
        parse_mode: HTML
        send_resolved: true
        message: &telegram_message_template |
          {{ if eq .Status "firing" }}🔥 <b>FIRING</b>{{ else }}✅ <b>RESOLVED</b>{{ end }} — {{ .CommonLabels.alertname }}
          {{ range .Alerts }}
          {{ if eq .Labels.severity "critical" }}🔴{{ else if eq .Labels.severity "warning" }}⚠️{{ else }}ℹ️{{ end }} <b>{{ .Labels.alertname }}</b> on <code>{{ .Labels.host }}</code>{{ if .Labels.host_ip }} (<code>{{ .Labels.host_ip }}</code>){{ end }}
          {{ if .Labels.attacker_ip }}   <b>Attacker:</b> <code>{{ .Labels.attacker_ip }}</code>
          {{ end }}   severity=<b>{{ .Labels.severity }}</b> · category={{ .Labels.category }}
             {{ .StartsAt.Format "2006-01-02 15:04:05 MST" }}

             {{ .Annotations.summary }}
             {{ if .Annotations.description }}<i>{{ .Annotations.description }}</i>{{ end }}

          → Investigate: VMUI query <code>host:{{ .Labels.host }}</code>{{ if .Labels.attacker_ip }} or <code>_msg:"{{ .Labels.attacker_ip }}"</code>{{ end }} · Claude+MCP for triage
          {{ end }}

  - name: telegram-event
    telegram_configs:
      - bot_token: "__TELEGRAM_BOT_TOKEN__"
        chat_id: __TELEGRAM_CHAT_ID__
        parse_mode: HTML
        send_resolved: false   # event-style: chỉ FIRING, không gửi RESOLVED
        message: *telegram_message_template

inhibit_rules: []
