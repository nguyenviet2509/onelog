[Journal]
# Cap systemd journal tổng size 500M (default = 10% of /var/log/journal → có thể
# lên ~10GB). 500M đủ cho ~2 tuần journal ở scale hiện tại (89M sau ~1 tháng
# uptime), giữ root disk headroom.
SystemMaxUse=500M
SystemMaxFileSize=100M
